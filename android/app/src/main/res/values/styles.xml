<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="android:editTextBackground">@drawable/screen</item>
        
        <!-- Android 12+ SplashScreen compatibility -->
        <item name="android:windowSplashScreenBackground">#bce5ea</item>
        <item name="android:windowSplashScreenAnimatedIcon">@drawable/text_taqeebat</item>
    </style>

</resources>
