## 🎯 Core Concept (Ascend Habits)

Users:

* create habits
* track daily progress
* build streaks
* see growth over time

So we need:

1. Users
2. Habits
3. Habit schedules
4. Habit tracking (daily logs)
5. Streaks & stats (derived, not stored heavily)

---

## 🧱 Core Tables (Minimal & Common)

### 1️⃣ users

Already exists if you use Laravel auth.

```sql
users
- id (PK)
- name
- email
- password
- created_at
- updated_at
```

---

### 2️⃣ habits

Each user can create many habits.

```sql
habits
- id (PK)
- user_id (FK → users.id)
- title
- icon
- description
- habit_type (boolean | count | time)
- target_value (nullable) -- e.g. 10 reps, 30 min
- is_active (boolean)
- created_at
- updated_at
```

**Relation**

* `users 1 → N habits`

---

### 3️⃣ habit_schedules

Defines **when** a habit should be done.

```sql
habit_schedules
- id (PK)
- habit_id (FK → habits.id)
- frequency (daily | weekly | custom)
- days (json) -- ["mon","wed","fri"]
- start_date
- end_date (nullable)
- created_at
```

**Why separate table?**

* Flexible scheduling
* Easy future upgrades (reminders, smart plans)

---

### 4️⃣ habit_logs (MOST IMPORTANT)

Tracks daily completion.

```sql
habit_logs
- id (PK)
- habit_id (FK → habits.id)
- log_date (date)
- value (nullable) -- count/minutes/etc
- is_completed (boolean)
- created_at
```

**Rules**

* One record per habit per day
* Unique index:

```sql
UNIQUE (habit_id, log_date)
```

---

### 5️⃣ habit_streaks (OPTIONAL but useful)

we can calculate streaks dynamically,
but storing improves performance.

```sql
habit_streaks
- id (PK)
- habit_id (FK → habits.id)
- current_streak
- longest_streak
- last_completed_date
- updated_at
```

---

## 🔗 Relationship Summary (Simple View)

```
users
  └── habits
        ├── habit_schedules
        ├── habit_logs
        └── habit_streaks
```

---
